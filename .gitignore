.terraform/
**/.terraform/*
*.tfstate
*.tfstate.*
.terraform.lock.hcl
*.zip
dev-current.tfstate
*.tfplan
tfplan
plan*.txt
**/plan*.txt
*.log

# Node.js (PDF generation)
node_modules/
package-lock.json

# IDE
.vscode/

# Local templates (não subir)
templates/

# Documentos internos soltos na raiz de docs/ (subpastas continuam versionadas)
docs/*.md
docs/*.txt



# Backup
static_site_backup/

# Planilhas/dados fonte soltos (ex.: escopo do exame) — não subir
*.xlsx
*.xls
*.csv

# Bancos de questões dos simulados (ficam locais, upload manual para S3)
**/questoes.js

PLANO_PROXIMAS_FASES.md

docs_local/
scripts/
specs/

static_site/admin/teste

# Artefatos de teste soltos (saída de invocações, etc.)
response.json
**/response.json
event.json
**/event.json

# ===== Segurança — nunca versionar arquivos sensíveis =====
# Credenciais e segredos
.env
.env.*
*.env
credentials
**/credentials
aws.json
secrets.json
*.secret
*.secrets

# Chaves e certificados
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
id_rsa
id_ed25519
*.ppk

# Config local do frontend (gerado pelo Terraform, contém IDs de infra)
static_site/config.js

# Credenciais AWS locais
.aws/
**/.aws/